dependencies {
    // spring
    implementation group: "org.springframework", name: "spring-context", version: "${springV}"
    // spring-boot
    implementation group: "org.springframework.boot", name: "spring-boot", version: "${springBootV}"
}